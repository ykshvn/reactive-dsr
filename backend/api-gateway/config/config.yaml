server:
  port: 6969
  read_timeout: 10s
  write_timeout: 30s
  idle_timeout: 60s

services:
  simulation_url: "http://localhost:6970"

rate_limit:
  requests_per_second: 100
  burst: 200

cors:
  allowed_origins: ["http://localhost:4200"]
